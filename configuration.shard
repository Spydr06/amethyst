{ pkgs, archive, ... }: {
    bootloader.limine = {
        enabled = true;
        uefi = true;
    };

    initrd = {
        enabled = true;
        compression = { method = archive.ustar; format = archive.uncompressed; }; 
    };

    pkgs = pkgs;
}

